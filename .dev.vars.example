# Worker environment variables (used by wrangler for local dev)
# These are injected by Doppler via `doppler run -- wrangler dev`
# Note: AUTH0_APP_* prefix distinguishes app credentials from Management API credentials

AUTH0_DOMAIN=https://your-tenant.auth0.com/
AUTH0_APP_CLIENT_ID=your-client-id
AUTH0_APP_CLIENT_SECRET=your-client-secret
AUTH0_APP_AUDIENCE=api://dashboard
